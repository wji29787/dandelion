<template>
  <div :class="['loading-mask', { 'loading-is-visible': visible }]">
    <div class="lds-ripple">
      <div></div>
      <div></div>
    </div>
  </div>
</template>

<script setup>
const props = defineProps({
  visible: {
    type: Boolean,
    default: false,
  },
});
</script>

<style lang="less" scoped>
.lds-ripple {
  display: inline-block;
  position: absolute;
  top: calc(50% - 40px);
  left: calc(50% - 40px);
  width: 80px;
  height: 80px;
  div {
    position: absolute;
    border: 4px solid #fff;
    opacity: 1;
    border-radius: 50%;
    animation: lds-ripple 1s cubic-bezier(0, 0.2, 0.8, 1) infinite;

    &:nth-child(2) {
      animation-delay: -0.5s;
    }
  }
}

@keyframes lds-ripple {
  0% {
    top: 36px;
    left: 36px;
    width: 0;
    height: 0;
    opacity: 1;
  }
  100% {
    top: 0px;
    left: 0px;
    width: 72px;
    height: 72px;
    opacity: 0;
  }
}

.loading-mask {
  position: fixed;
  width: 100%;
  height: 100%;
  z-index: 9999;
  visibility: hidden;
  background-color: rgba(#0a0a0a, 0.5);
  opacity: 0;
  transition: visibility 0s, opacity 0.2s linear;

  &.loading-is-visible {
    visibility: visible;
    opacity: 1;
  }
}
</style>